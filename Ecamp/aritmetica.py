a = 20 #definimos primera variable y asignamos valor indicado
b = 30 #definimos segunda variable y asignamos valor indicado 
resultado = a * b # definimos variable resultado y le asignamos el valor de la multiplicación de a + b
print(resultado) # finalmete imprimimos el resultado, esto mostrará el resultado de la multiplicación de los valores asignados a las variables